package exercise_13

// WordCount counts the frequency of each word in a string slice.
// TODO: build a map[string]int
func WordCount(words []string) map[string]int {
	return nil
}

// Lookup returns the value for key and whether the key existed.
// TODO: use the comma-ok idiom
func Lookup(m map[string]int, key string) (int, bool) {
	return 0, false
}

// Invert swaps keys and values in a map[string]string.
// TODO: build a new map with swapped pairs
func Invert(m map[string]string) map[string]string {
	return nil
}
