package exercise_11

// FirstThree returns the first three elements of a slice.
// If the slice has fewer than 3 elements, return all of them.
// TODO: use slice expression s[:n]
func FirstThree(s []int) []int {
	return nil
}

// ArraySum returns the sum of a fixed-size [5]int array.
// TODO: iterate over the array
func ArraySum(arr [5]int) int {
	return 0
}

// SliceInfo returns the length and capacity of a slice.
// TODO: use len() and cap()
func SliceInfo(s []int) (length, capacity int) {
	return 0, 0
}

// RemoveLast returns a new slice with the last element removed.
// Returns an empty slice if input is empty.
// TODO: use slice expression
func RemoveLast(s []int) []int {
	return nil
}
