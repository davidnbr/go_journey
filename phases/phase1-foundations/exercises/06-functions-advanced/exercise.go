package exercise_06

// Sum is a variadic function that returns the sum of all arguments.
// TODO: iterate over nums and sum them
func Sum(nums ...int) int {
	return 0
}

// Apply takes a function and applies it to a value.
// TODO: call fn(x) and return the result
func Apply(x int, fn func(int) int) int {
	return 0
}

// Multiplier returns a function that multiplies its argument by factor.
// TODO: return a closure that multiplies by factor
func Multiplier(factor int) func(int) int {
	return nil
}
