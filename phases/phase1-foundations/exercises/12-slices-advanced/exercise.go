package exercise_12

// Deduplicate returns a new slice with duplicate integers removed,
// preserving original order.
// TODO: use a map to track seen values
func Deduplicate(s []int) []int {
	return nil
}

// Flatten takes a slice of slices and returns a single flat slice.
// TODO: append each sub-slice into the result
func Flatten(ss [][]int) []int {
	return nil
}

// CopySlice returns a new independent copy of the slice.
// Modifying the returned slice must NOT affect the original.
// TODO: use make + copy (not just s[:])
func CopySlice(s []int) []int {
	return nil
}
