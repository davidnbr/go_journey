package exercise_08

// Classify returns "negative", "zero", or "positive" for a given int.
// TODO: use if/else if/else
func Classify(n int) string {
	return ""
}

// AbsoluteValue returns the absolute value of n.
// TODO: return n if positive, -n if negative
func AbsoluteValue(n int) int {
	return 0
}

// ParseScore parses a score string and returns a grade letter using if-with-init.
// Scores: >= 90 = "A", >= 80 = "B", >= 70 = "C", >= 60 = "D", else "F"
// Returns "invalid" if the score is not in [0, 100].
// TODO: implement using if/else if chain; handle out-of-range
func ParseScore(score int) string {
	return ""
}
