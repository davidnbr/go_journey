package exercise_05

// Add returns the sum of two integers.
// TODO: return a + b
func Add(a, b int) int {
	return 0
}

// Divide returns the quotient and a boolean indicating success.
// Returns (0, false) if divisor is zero.
// TODO: handle division by zero, return (a/b, true) or (0, false)
func Divide(a, b int) (int, bool) {
	return 0, false
}

// MinMax returns the minimum and maximum of a slice.
// Returns (0, 0) for empty slice.
// TODO: iterate and find min/max
func MinMax(nums []int) (min, max int) {
	return 0, 0
}
